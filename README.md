# skills

A library of [Claude Code](https://claude.ai/code) skills — modular packages that extend Claude's capabilities with specialized workflows, tool integrations, and domain knowledge.

## Structure

Each skill is a directory containing a `SKILL.md` with YAML frontmatter and instructions, plus optional `scripts/`, `references/`, and `assets/` subdirectories.
